<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- default -->
    <item android:state_enabled="true" >
        <shape>
            <gradient
                android:startColor="@color/white"
                android:endColor="@color/white"
                android:angle="270" />

            <stroke android:width="1dp" android:color="@color/secondary_light_blue" />
            <corners android:radius="@dimen/radius_component" />
        </shape>
    </item>

    <!-- focused -->
    <item android:state_focused="true">
        <shape>
            <gradient
                android:startColor="@color/white"
                android:endColor="@color/white"
                android:angle="270" />

            <stroke android:width="1dp" android:color="@color/secondary_light_blue" />
            <corners android:radius="@dimen/radius_component" />
        </shape>
    </item>

    <!-- pressed -->
    <item android:state_pressed="false">
        <shape>
            <gradient
                android:startColor="#75A0F3"
                android:endColor="#75A0F3"
                android:angle="90" />

            <stroke android:width="1dp" android:color="@color/secondary_light_blue" />
            <corners
                android:radius="@dimen/radius_component" />
        </shape>
    </item>
</selector>
